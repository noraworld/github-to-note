## 仕様周り
* [ ] note には投稿しない機能の開発
    * 記事ファイル内に `note_disabled: true` のような YAML ヘッダが存在した場合に note への投稿をスキップする（仕様については要検討）

## エディタ周り
* [ ] 「ここから先は有料部分です」を挿入する機能の開発
    * 下書き保存時のネットワークを解析する必要がある
    * 仕様としては記事ファイル内で特殊な HTML コメントを用意することで実現する
    * 「ここから先は有料部分です」を記事内に埋め込んでいたとしても、公開時に「無料」を選択すると無効になってしまうのでそれも考慮する必要がある
    * 有料記事を公開するには最初に住所登録などを行う必要がある
* [ ] https://example.com のように裸の URL を貼ったときにリンクとして認識されるようにする
    * すでに [Example.com](https://example.com) のようにタイトルをつければリンクになることは確認済みなので、あとは実装側で裸の URL をそのように変換すれば良い
* [ ] その行に URL 単体を書いた場合に「埋め込み」として表示されるようにする
    * note エディタ上では自動でこのような表示になるが `[https://example.com](https://example.com)` のようにリンクになっていれば自動でそうなるのか、あるいは明示的に特殊な HTML を使う必要があるのかは調査・検証する必要がある

## 認証周り
* [ ] ~~GitHub Actions 上でログイン時のアクセス制限がかかった場合にセッション情報を自動で使うようにする~~
    * ~~ローカル上でコマンドを直接実行する際は `.env` のそれらを使用するようになっているが、GitHub Actions 上ではまだ使えるようになっていないので改修する必要がある~~
    * どうやらすでに使えるようになっていたっぽいが、ワークフロー内で使えることを示す文言が README には入っていなかったので追記したほうがわかりやすいだろう（開発環境用に `.env` に入れることについては明記されている）
* [ ] ID とパスワードではなく、最初からセッション情報を使うようにするオプションを作る
    * セッション情報がある場合、ID とパスワードでのログインに失敗したら自動でセッション情報を使うようになっているが、その状況下においては常に ID とパスワードでのログインの失敗までのタイムアウト時間を待たなければいけないのと、それを毎回試すことで制限がかかっている時間が延長される恐れがあるため、最初からセッション情報を使うようなモードを用意しておきたい（特にローカル環境開発時）
